# Superseded — STG is not a promotion target

We do **not** promote through STG (`stg` branch, titanio-stg, `stg.hashpower.exchange`).

The path is **`dev` (testnet) → `main` (mainnet)**.

Use **[dev-main-promotion-runbook.md](./dev-main-promotion-runbook.md)** for hashpower.exchange, hashpower.io, and mcp.hashpower.io.

The June 2026 STG audit (Goldsky `STG_*` vars, contested `keeper.stg` / `perpsmm.stg` DNS, `03-stg` apply order) is historical only. Do not execute it.
