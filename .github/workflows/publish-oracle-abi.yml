name: Publish @hashpower/oracle-abi

# Publishes the ABI package to npm whenever the generated ABIs or the
# deployments manifest change on main. Versioning is automatic and
# semver-correct: the ABI *is* the package's public API, so CI diffs the
# built ABI surface against the last published version to compute the bump
# (removed/changed entry -> major, added entry -> minor, metadata -> patch)
# — demoted while the published major is 0, per semver's 0.x convention
# (breaking -> minor, additive -> patch) — and publishes with that version. Nothing is committed back — the branch is
# protected — so the version of record lives on npm and each release is
# marked with a git tag (oracle-abi-vX.Y.Z) on the source commit.
#
# Auth uses npm Trusted Publishing (OIDC) — no NPM_TOKEN secret.
# NOTE: the very first release must be published manually
# (`cd oracle-abi && pnpm build && npm publish --access public`),
# then configure this repo+workflow as a Trusted Publisher in the
# package settings on npmjs.com. CI handles every release after that.

on:
  push:
    branches:
      # NOTE: publishing off `dev` while the repo operates there.
      # At GA, change this to `main` (and update the `npm-publish`
      # environment's deployment-branch rule to match) so npm only
      # updates on mainline releases.
      - dev
    paths:
      - "contracts/abi/**"
      - "oracle-abi/**"
      - ".github/workflows/publish-oracle-abi.yml"
  # Manual runs from any branch for testing the pipeline. `version` publishes
  # exactly that version, skipping the ABI diff (one-time resets / recovery).
  workflow_dispatch:
    inputs:
      version:
        description: "Exact version to publish (skips semver diff)"
        required: false
        default: ""

permissions:
  contents: write # push release tags
  id-token: write # npm provenance / trusted publishing

concurrency:
  group: publish-oracle-abi
  cancel-in-progress: false

jobs:
  publish:
    name: 📦 Build & publish to npm
    runs-on: ubuntu-latest
    # Must match the Trusted Publisher environment configured on npmjs.com
    environment: npm-publish
    defaults:
      run:
        working-directory: ./oracle-abi

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: oracle-abi/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
          cache: "pnpm"
          cache-dependency-path: oracle-abi/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package
        run: pnpm build

      - name: Compute semver bump from ABI diff
        id: bump
        env:
          FORCED_VERSION: ${{ inputs.version || '' }}
        run: |
          if [ -n "$FORCED_VERSION" ]; then
            npm version "$FORCED_VERSION" --no-git-tag-version --allow-same-version
            echo "Forced version: $FORCED_VERSION (semver diff skipped)"
            echo "level=forced" >> "$GITHUB_OUTPUT"
            echo "version=$FORCED_VERSION" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          PKG=$(node -p "require('./package.json').name")
          PUBLISHED=$(npm view "$PKG" version 2>/dev/null || echo "none")

          if [ "$PUBLISHED" = "none" ]; then
            echo "First publish — using version from package.json as-is"
            echo "level=first" >> "$GITHUB_OUTPUT"
            echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Fetch the published tarball and diff its ABI surface against the fresh build
          TARBALL=$(npm pack "$PKG@$PUBLISHED" --silent | tail -1)
          mkdir -p /tmp/published
          tar -xzf "$TARBALL" -C /tmp/published
          rm "$TARBALL"

          LEVEL=$(node scripts/semver-diff.mjs /tmp/published/package .)
          echo "Published: $PUBLISHED — ABI diff requires: $LEVEL"

          # Pre-1.0 the package is explicitly unstable (semver 0.x rules), so
          # demote: breaking -> minor, additive -> patch. Cutting 1.0.0 at GA
          # (from main) ends the demotion automatically.
          if [ "${PUBLISHED%%.*}" = "0" ]; then
            case "$LEVEL" in
              major) LEVEL=minor ;;
              minor) LEVEL=patch ;;
            esac
            echo "0.x pre-release line — demoted bump to: $LEVEL"
          fi
          echo "level=$LEVEL" >> "$GITHUB_OUTPUT"

          if [ "$LEVEL" = "none" ]; then
            echo "No ABI or metadata changes — skipping publish"
            echo "version=$PUBLISHED" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Baseline on the published version, then apply the computed bump.
          # Working-copy only: dev is protected (PRs required) so CI never
          # pushes commits. npm holds the version of record.
          npm version "$PUBLISHED" --no-git-tag-version --allow-same-version
          npm version "$LEVEL" --no-git-tag-version
          NEW=$(node -p "require('./package.json').version")
          echo "version=$NEW" >> "$GITHUB_OUTPUT"
          echo "Version: $NEW ($LEVEL bump from $PUBLISHED)"

      - name: Publish
        if: steps.bump.outputs.level != 'none'
        # --tag latest is explicit so a forced lower version (reset/rollback)
        # can still take the latest tag; npm forbids that implicitly.
        run: npm publish --access public --provenance --tag latest

      - name: Tag release
        if: steps.bump.outputs.level != 'none'
        continue-on-error: true # traceability only — never blocks a publish
        run: |
          TAG="oracle-abi-v${{ steps.bump.outputs.version }}"
          git tag "$TAG"
          git push origin "$TAG"

      - name: Summary
        if: steps.bump.outputs.level != 'none'
        run: |
          PKG=$(node -p "require('./package.json').name")
          VERSION=$(node -p "require('./package.json').version")
          echo "## 📦 Published $PKG@$VERSION" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "https://www.npmjs.com/package/$PKG/v/$VERSION" >> $GITHUB_STEP_SUMMARY

      - name: Send Slack notification
        if: always() && steps.bump.outputs.level != 'none' && steps.bump.outputs.level != ''
        uses: ./.github/actions/slack-notify
        with:
          status: ${{ job.status }}
          environment: "npm"
          service_name: "@hashpower/oracle-abi"
          version: ${{ steps.bump.outputs.version || 'unknown' }}
          slack_webhook_url: ${{ secrets.SLACK_WEBHOOK_URL }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          additional_info: "${{ format('*Semver bump:* `{0}` • *Package:* <https://www.npmjs.com/package/@hashpower/oracle-abi/v/{1}|@hashpower/oracle-abi@{1}>', steps.bump.outputs.level, steps.bump.outputs.version) }}"
